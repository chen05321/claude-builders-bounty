# Weekly Dev Summary — n8n + Claude

Automated weekly narrative summary of a GitHub repo's activity, generated by Claude and delivered to your team's Slack/Discord/email.

## Setup (5 steps)

### 1. Import the workflow

1. Open your n8n instance (cloud or self-hosted)
2. Go to **Workflows** → **Import from File**
3. Select `weekly-dev-summary.json`

### 2. Connect GitHub

1. In the three **GitHub** nodes, click **Create New Credential**
2. Select **OAuth2** → authenticate with your GitHub account
3. Ensure the token has `repo` scope for private repos (or just `public_repo` for public)

### 3. Add Claude API Key

1. Get an API key from [console.anthropic.com](https://console.anthropic.com)
2. In the **Claude API** node → **Create New Credential**
3. Choose **Header Auth** → set:
   - **Header Name**: `x-api-key`
   - **Header Value**: your Anthropic API key

### 4. Configure Delivery

Decide where the summary goes:

| Destination | Config |
|-------------|--------|
| **Slack** | Create a [Slack webhook](https://api.slack.com/messaging/webhooks) → paste URL in **Config** node |
| **Discord** | Create a [Discord webhook](https://support.discord.com/hc/en-us/articles/228383668) → paste URL in **Config** node |
| **Email** | Delete the **Webhook Delivery** node, add an **Email** node (SMTP), wire it after **Format Output** |

Update these fields in the **⚙️ Config** node:
- `webhookUrl` — your Slack/Discord webhook URL
- `repoOwner` — GitHub org or user (e.g. `vercel`)
- `repoName` — repository name (e.g. `next.js`)
- `language` — `EN` (English) or `FR` (Français)
- `timeRangeDays` — lookback window (default: `7`)

### 5. Activate

1. Click **Save** ☝️
2. Toggle **Active** on
3. First run happens on the next Friday at 5pm
4. Or click **Execute Workflow** to test immediately

## What to Expect

A rich message like this lands in your channel every Friday:

```
📊 Weekly Dev Summary: vercel/next.js

Week ending May 15 | Repository
━━━━━━━━━━━━━━━━━━━━━━━━━

This week the team focused on optimizing the Turbopack integration
and addressing community-reported issues...

**Key Changes**
- ✨ Added support for React Server Components in App Router
- 🐛 Fixed memory leak in development mode (#71234)
- 🔄 Refactored image optimization pipeline

**Focus Areas**
- Turbopack stability: 12 PRs merged
- Community issues: 8 closed, 5 with reproduction accepted

**Looking Ahead**
- Partial Prerendering beta expansion expected next week
━━━━━━━━━━━━━━━━━━━━━━━━━
🤖 Generated by n8n + Claude
```

## Architecture

```
📅  Cron (Fri 5pm)
  │
⚙️  Config → repoOwner, repoName, webhookUrl, language
  │
  ├── 📡 GitHub Commits (since last 7 days)
  ├── 📡 GitHub Closed Issues
  └── 📡 GitHub Merged PRs
  │
🧩  Build Prompt → combine data + generate Claude prompt
  │
🤖  Claude API → claude-sonnet-4-20250514
  │
📝  Format Output → Slack Blocks / Markdown
  │
📤  Webhook → Slack / Discord / Teams
```

## Requirements

- **n8n** v1.0+ (cloud or self-hosted)
- **GitHub** account (free) — for API access
- **Anthropic** API key — [get one here](https://console.anthropic.com)
- **Webhook URL** — Slack (preferred), Discord, or Teams

## Troubleshooting

| Problem | Fix |
|---------|-----|
| Claude API returns 401 | Check your API key in the credential |
| GitHub API returns 403 | Token missing `repo` scope — regenerate it |
| Webhook returns 404 | Webhook URL might be expired — create a new one |
| "No data" in summary | `timeRangeDays` too small, or repo had no activity |
| Timezone off | Change workflow **Settings** → **Timezone** |

## Tested

✅ Import to n8n cloud — successful  
✅ All nodes validate without errors  
✅ Claude API connection — confirmed  
✅ Slack webhook delivery — confirmed  

Replace the placeholder values in **⚙️ Config** with your own before activating.
